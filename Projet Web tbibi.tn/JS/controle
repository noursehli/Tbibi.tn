function agePatient(f , a) {
if (age.value < 18){
alert("le patient ne peut pas prendre un rendez-vous")}
else
{
alert ("vous pouvez prendre un rendez-vous");
f.submit();
}

function Verifmdp(m) {
if (m.value.length<9) {
 alert("Le mot de passe " + m.name + " doit contenir au moins 9 caractères");
 m.focus();
return false; }
 else{
return true;
 }
